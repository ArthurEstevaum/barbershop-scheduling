export interface CustomerModelForm {
  id: number,
  name: string,
  email: string,
  phoneNumber: string
}

export interface CustomerModelTable {
  id: number,
  name: string,
  email: string,
  phoneNumber: string
}
